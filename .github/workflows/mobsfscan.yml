name: mobsfscan

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  mobile-audit:
    name: "Trigger Mobile Audit" # modify to suit project name
    uses: Xttreme/cross-github-actions/.github/workflows/mobile-scan.yaml@dev
    with:
      runs-on: ubuntu-latest
